# Run Doc — Yöresel Beyaz Tahta

## How to reproduce uncommitted artifacts
1. `npm install` (dependencies)
2. No `.env.local` needed — in-memory store only

## How to run the server
```bash
npm run dev
```
Default port: 3000 (Next.js default). If occupied, the dev server picks the next free port.
